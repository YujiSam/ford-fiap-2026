global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "ford-ic-api"
    metrics_path: /metrics
    authorization:
      credentials_file: /etc/prometheus/metrics_token.txt
    static_configs:
      - targets: ["host.docker.internal:8000"]
